[project]
name = "${project_name}"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    #"gentleman>=${gentleman_version}",
    "gentleman",
]


[tool.uv.sources]
gentleman = { git = "https://github.com/madebyman/gentleman" }
